# Copy to .env and fill in. .env is gitignored.

# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Dropbox app (Full Dropbox scope — NOT app-folder, see FR-3)
DROPBOX_APP_KEY=
# PKCE flow, no secret needed on mobile

# LLM (pick one; provider chosen in llm_service.dart)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
